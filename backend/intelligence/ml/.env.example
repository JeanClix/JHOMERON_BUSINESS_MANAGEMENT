# Reutiliza el rol de solo lectura ai_readonly (ver backend/batch/.../schema-ai.sql)
# Solo puede leer ai.v_ventas / ai.v_ventas_mensual_departamento -- suficiente
# para feature engineering, sin abrir permisos nuevos sobre dwh./staging.
DATABASE_URL=postgresql://ai_readonly:ai_readonly_local_dev@localhost:5432/jhomeron_batch

# MLflow: tracking + registry en SQLite local para desarrollo.
MLFLOW_TRACKING_URI=sqlite:///mlflow.db
