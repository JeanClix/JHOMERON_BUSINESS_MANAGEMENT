/**
 * URL base del AI Service (FastAPI, backend/intelligence/ai).
 * En dev corre en localhost:8090 (uvicorn). Ajustar aquí cuando exista
 * un dominio de despliegue real, o migrar a environment.ts si el proyecto
 * adopta ese esquema más adelante.
 */
export const AI_SERVICE_BASE_URL = 'http://localhost:8090';
